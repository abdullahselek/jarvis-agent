[project]
name = "jarvis-agent"
version = "0.1.0"
description = "A local-first, multi-agent AI system using hermes-agent and ollama."
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["pyyaml"]

[project.scripts]
jarvis = "main:main"
